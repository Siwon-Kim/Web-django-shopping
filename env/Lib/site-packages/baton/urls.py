from django.urls import include, re_path

from .views import GetAppListJsonView, GetGravatartUrlJsonView


urlpatterns = [
    re_path(r'^app-list-json/$', GetAppListJsonView.as_view(),
        name='baton-app-list-json'),
    re_path(r'^gravatar/$', GetGravatartUrlJsonView.as_view(),
        name='baton-gravatar-json'),
]
